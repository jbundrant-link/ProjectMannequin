# User Content

Local creator content will be scanned from this folder.

For the first prototype, character packs are declarative only. A character folder should eventually look like:

```text
UserContent/
  Characters/
    MyCharacter/
      character.json
      moveset.json
```

Invalid packs are disabled and reported instead of crashing the game.
